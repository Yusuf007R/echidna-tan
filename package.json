{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "docker": "turbo run docker",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "yarn workspace ui ui:add",
    "ui:shadcn": "yarn workspace ui ui:shadcn"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "name": "echidna-tan",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
